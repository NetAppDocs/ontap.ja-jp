---
permalink: consistency-groups/configure-task.html 
sidebar: sidebar 
keywords: consistency groups, consistency group, application protection, parent, child, configure 
summary: アプリケーション ワークロード管理とデータ保護のために単一の整合性グループを作成します。 
---
= 単一のONTAPコンシステンシグループを構成する
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
整合性グループは、ONTAPのバージョンに応じて、既存のボリュームまたは新しいLUN / ボリュームを使用して作成できます。ボリュームまたはLUNを関連付けることのできる整合性グループは一度に1つだけです。

.タスク概要
* ONTAP 9.10.1～9.11.1では、整合性グループの作成後にメンバー ボリュームを変更することはできません。
+
ONTAP 9.12.1以降では、コンシステンシーグループのメンバーボリュームを変更できるようになりました。このプロセスの詳細については、xref:modify-task.html[整合性グループの変更]を参照してください。

* ONTAP 9.17.1 以降では、NVMe プロトコルを選択して、SnapMirror アクティブ同期構成で VMware ワークロードの NVMe サブシステムにホストをマッピングできます。




== 新しいLUNまたはボリュームを使用した整合性グループの作成

ONTAP 9.10.1～9.12.1では、新しいLUNを使用して整合性グループを作成できます。ONTAP 9.13.1以降のSystem Managerでは、整合性グループの作成に新しいNVMeネームスペースまたは新しいNASボリュームも使用できます。（これは、ONTAP 9.12.1以降のONTAP REST APIでもサポートされています）。

[role="tabbed-block"]
====
.System Manager（ONTAP 9.16.1以前）
--
.手順
. *Storage > Consistency groups*を選択します。
. *+追加*を選択し、ストレージ オブジェクトのプロトコルを選択します。
+
ONTAP 9.10.1～9.12.1では、新しいストレージオブジェクトのオプションは**新しいLUNを使用する**のみです。ONTAP 9.13.1以降、System Managerは新しいNVMeネームスペースと新しいNASボリュームを使用したコンシステンシグループの作成をサポートします。

. 整合性グループに名前を付けます。ボリュームまたはLUNの数とボリュームまたはLUNあたりの容量を指定します。
+
.. **アプリケーションタイプ**：ONTAP 9.12.1以降を使用している場合は、アプリケーションタイプを選択してください。値が選択されていない場合、整合性グループにはデフォルトで**その他**のタイプが割り当てられます。整合性のタグ付けの詳細については、xref:modify-tags-task.html[アプリケーション タグとコンポーネント タグ]を参照してください。リモート保護ポリシーを使用して整合性グループを作成する場合は、*その他*を使用する必要があります。
.. **新しいLUN**の場合：ホストのオペレーティングシステムとLUNフォーマットを選択します。ホストイニシエータ情報を入力します。
.. **新しい NAS ボリューム**の場合：SVM の NAS 構成に基づいて適切なエクスポート オプション（NFS または SMB/CIFS）を選択します。
.. **新しい NVMe 名前空間**の場合：ホスト オペレーティング システムと NVMe サブシステムを選択します。


. 保護ポリシーを構成したり、子整合性グループやアクセス権限を追加したりするには、*その他のオプション*を選択します。
. *保存*を選択します。
. メインの整合性グループ メニューに戻り、整合性グループが作成されたことを確認します。保護ポリシーを設定した場合は、該当するリモートまたはローカルのポリシーに表示される緑色の盾アイコンで、保護ポリシーが適用されたことを確認できます。


--
.System Manager（ONTAP 9.17.1以降）
--
.手順
. *保護 > 整合性グループ*を選択します。
. *+追加*を選択し、ストレージ オブジェクトのプロトコルを選択します。
. 整合性グループに名前を付けます。ボリュームまたはLUNの数と、ボリュームまたはLUNあたりの容量を指定します。**アプリケーションタイプ**：アプリケーションタイプを選択します。値が選択されていない場合、整合性グループにはデフォルトで**その他**のタイプが割り当てられます。整合性のタグ付けの詳細については、xref:modify-tags-task.html[アプリケーション タグとコンポーネント タグ]を参照してください。リモート保護ポリシーを使用して整合性グループを作成する場合は、*その他*を使用する必要があります。
+
.. **新しいLUN**の場合：ホストのオペレーティングシステムとLUNフォーマットを選択します。ホストイニシエータ情報を入力します。
.. **新しい NAS ボリューム**の場合：SVM の NAS 構成に基づいて適切なエクスポート オプション（NFS または SMB/CIFS）を選択します。
.. **新しい NVMe 名前空間**の場合：ホスト オペレーティング システムと NVMe サブシステムを選択します。


. 保護ポリシーを構成したり、子整合性グループやアクセス権限を追加したりするには、*その他のオプション*を選択します。
. *保存*を選択します。
. メインの整合性グループ メニューに戻り、整合性グループが作成されたことを確認します。保護ポリシーを設定した場合は、該当するリモートまたはローカルのポリシーに表示される緑色の盾アイコンで、保護ポリシーが適用されたことを確認できます。


--
.CLI
--
ONTAP 9.14.1以降では、ONTAP CLIを使用して、新しいボリュームを使用した新しい整合性グループを作成できます。指定するパラメータは、SAN、NVMe、NFSのうち、どのボリュームを使用するかによって異なります。

.開始する前に
* このタスクを実行するには、admin権限レベルが必要です。
* ONTAP 9.15.1以降では、admin権限レベルのすべてのユーザがこのタスクを実行できます。ONTAP 9.14.1では、このタスクを実行するには、クラスタ管理者またはSVMの管理者である必要があります。


.NFSボリュームを使用した整合性グループの作成
. 整合性グループを作成します。
+
`consistency-group create -vserver <SVM_name> -consistency-group <consistency-group-name> -volume-prefix <prefix_for_new_volume_names> -volume-count <number> -size <size> -export-policy <policy_name>`



.SANボリュームを使用した整合性グループの作成
. 整合性グループを作成します。
+
`consistency-group create -vserver <SVM_name> -consistency-group <consistency-group-name> -lun <lun_name> -size <size> -lun-count <number> -lun-os-type <LUN_operating_system_format> -igroup <igroup_name>`



.NVMeネームスペースを使用した整合性グループの作成
. 整合性グループを作成します。
+
`consistency-group create -vserver <SVM_name> -consistency-group <consistency_group_name> -namespace <namespace_name> -volume-count <number> -namespace-count <number> -size <size> -subsystem <subsystem_name>`

+
link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=consistency-group+create["ONTAPコマンド リファレンス"^]の `consistency-group create`の詳細をご覧ください。



.完了後
.  `consistency-group show`コマンドを使用して、整合性グループが作成されたことを確認します。
+
 `consistency-group show`の詳細については、link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=consistency-group+show["ONTAPコマンド リファレンス"^]を参照してください。



--
====


== 既存のボリュームを使用した整合性グループの作成

既存のボリュームを使用して整合性グループを作成できます。

[role="tabbed-block"]
====
.System Manager（ONTAP 9.16.1以前）
--
.手順
. *Storage > Consistency groups*を選択します。
. *+追加*を選択し、次に*既存のボリュームを使用*を選択します。
. 整合性グループに名前を付け、Storage VMを選択します。
+
.. **アプリケーションタイプ**：ONTAP 9.12.1以降をご使用の場合は、アプリケーションタイプを選択してください。値が選択されていない場合、コンシステンシーグループにはデフォルトで**その他**のタイプが割り当てられます。コンシステンシーのタグ付けの詳細については、xref:modify-tags-task.html[アプリケーション タグとコンポーネント タグ]を参照してください。コンシステンシーグループにSnapMirrorアクティブ同期関係がある場合は、*その他*を使用する必要があります。
+

NOTE: ONTAP 9.15.1より前のバージョンのONTAPでは、SnapMirrorアクティブ同期をSnapMirrorビジネス継続性と呼びます。



. グループに追加する既存のボリュームを選択します。選択できるのは、整合性グループに含まれていないボリュームのみです。
+

NOTE: 既存のボリュームで作成する整合性グループでは、FlexVolがサポートされます。SnapMirror同期関係またはSnapMirror非同期関係が設定されたボリュームは整合性グループには対応していませんが、整合性グループに追加することはできます。整合性グループでは、S3バケットまたはSVM DR関係が確立されたStorage VMはサポートされません。

. *保存*を選択します。
. メインの整合性グループのメニューに戻り、整合性グループが作成されたことを確認します。保護ポリシーを選択した場合は、メニューから作成した整合性グループを選択して、ポリシーが適切に設定されていることを確認します。保護ポリシーを設定した場合は、該当するリモートまたはローカルのポリシーに表示される緑色の盾アイコンで、保護ポリシーが適用されたことを確認できます。


--
.CLI
--
ONTAP 9.14.1以降では、ONTAP CLIを使用して、既存のボリュームを使用した整合性グループを作成できます。

.開始する前に
* このタスクを実行するには、admin権限レベルが必要です。
* ONTAP 9.15.1以降では、admin権限レベルのすべてのユーザがこのタスクを実行できます。ONTAP 9.14.1では、このタスクを実行するには、クラスタ管理者またはSVMの管理者である必要があります。


.手順
.  `consistency-group create`コマンドを発行します。 `-volumes`パラメータには、ボリューム名をカンマで区切って指定できます。
+
`consistency-group create -vserver <SVM_name> -consistency-group <consistency-group-name> -volume <volumes>`

+
link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=consistency-group+create["ONTAPコマンド リファレンス"^]の `consistency-group create`の詳細をご覧ください。

.  `consistency-group show`コマンドを使用して整合性グループを表示します。
+
 `consistency-group show`の詳細については、link:https://docs.netapp.com/us-en/ontap-cli/search.html?q=consistency-group+show["ONTAPコマンド リファレンス"^]を参照してください。



--
====
.次の手順
* xref:protect-task.html[整合性グループの保護]
* xref:modify-task.html[整合性グループの変更]
* xref:clone-task.html[整合性グループのクローニング]

