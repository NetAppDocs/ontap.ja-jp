---
permalink: flexgroup/disable-svm-protection-task.html 
sidebar: sidebar 
keywords: disable, SVM, protection, flexgroup, volume 
summary: FlexGroup ボリューム上で SVM DR フラグが保護に設定されている場合、フラグを非保護に設定して、FlexGroup ボリューム上の SVM DR 保護を無効にすることができます。 
---
= ONTAP FlexGroupボリューム上のSVM保護を無効にする
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
SVM DR フラグが `protected`FlexGroupボリュームで設定されている場合、フラグを非保護に設定して `protection`FlexGroupボリュームで SVM DR を無効にすることができます。

.開始する前に
* プライマリとセカンダリの間のSVM DR関係が健全である。
* SVM DR 保護パラメータが `protected`に設定されています。


.手順
.  `volume modify`コマンドを使用してFlexGroupボリュームの `vserver-dr-protection`パラメータを `unprotected`に変更し、保護を無効にします。
+
[listing]
----
cluster2::> volume modify -vserver vs1 -volume fg_src -vserver-dr-protection unprotected
[Job 5384] Job is queued: Modify fg_src.
[Job 5384] Steps completed: 4 of 4.
cluster2::>
----
. セカンダリ サイトの SVM を更新します： `snapmirror update -destination-path destination_svm_name: -source-path Source_svm_name:`
. SnapMirror関係が正常であることを確認します： `snapmirror show`
. FlexGroup SnapMirror関係が削除されたことを確認します： `snapmirror show -expand`


.関連情報
* link:https://docs.netapp.com/us-en/ontap-cli/snapmirror-show.html["snapmirror show"^]
* link:https://docs.netapp.com/us-en/ontap-cli/snapmirror-update.html["snapmirror update"^]

