---
permalink: nfs-admin/nconnect-concept.html 
sidebar: sidebar 
keywords: nconnect, nfs, performance, tcp connections, nfsv3, nfsv4.2 nfsv4.1 
summary: nconnect は、単一のマウント ポイントに対して複数の TCP 接続を有効にすることで NFS パフォーマンスを向上させます。 
---
= NFSパフォーマンスのためのnconnectについて学ぶ
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
ONTAP 9.8 以降では、NFSv4.1 が有効になっている場合、nconnect 機能がデフォルトで使用できます。nconnect は、単一のマウントに対して複数の TCP 接続を許可することで、NFS クライアントのパフォーマンスを向上させます。



== nconnectの仕組み

以前のNFSクライアント実装では、マウントでTCP接続を1つしか使用しません。ONTAPでは、1つのTCP接続がIOPSの増加に伴うボトルネックになる可能性があります。

nconnect 対応クライアントは、単一の NFS マウントに複数の TCP 接続 (最大 16 個) を関連付けることができます。nconnect は 1 つの IP アドレスのみを使用し、その単一の IP を介して複数の TCP 接続を確立して NFS エクスポートをマウントします。NFS クライアントは、ファイル操作をラウンドロビン方式で複数の TCP 接続に分散し、利用可能なネットワーク帯域幅からより高いスループットを実現します。



== サポートされるNFSバージョン

* NFSv3、NFSv4.2、および NFSv4.1 マウントには nconnect が推奨されます。
* nconnect は NFSv4.0 マウントには推奨されません。
+

NOTE: 最適なパフォーマンスを得るために、 NetAppNFSv4.0 ではなく nconnect を使用した NFSv4.1 の使用を推奨しています。NFSv4.0 は複数の接続をサポートしますが、nconnect を使用した NFSv4.1 では負荷分散が向上し、スループットが向上します。





== クライアントのサポート

NFS クライアントのマニュアルを参照して、 nConnect がクライアントバージョンでサポートされているかどうかを確認してください。



== 関連情報

* link:ontap-support-nfsv41-concept.html["ONTAPのNFSv4.1サポートについて"]
* link:ontap-support-nfsv42-concept.html["ONTAPのNFSv4.2サポートについて"]

