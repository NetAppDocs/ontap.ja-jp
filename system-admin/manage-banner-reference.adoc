---
permalink: system-admin/manage-banner-reference.html 
sidebar: sidebar 
keywords: manage, banner, svm, cluster, configure 
summary: バナーをクラスタまたはSVMレベルで管理し、バナーの表示方法を変更できます 
---
= ONTAPクラスタおよびSVMレベルで表示されるバナーテキストを管理する
:allow-uri-read: 
:icons: font
:imagesdir: ../media/


[role="lead"]
バナーはクラスタレベルまたはSVMレベルで管理できます。クラスタに設定されたバナーは、バナーメッセージが定義されていないすべてのSVMにも使用されます。その後にSVMに作成されたバナーは、そのSVMのクラスタバナーをオーバーライドします。

.オプション
* クラスタ レベルでバナーを管理する場合：
+
|===
| 状況 | 操作 


 a| 
すべてのログインセッションに表示されるバナーを作成する
 a| 
クラスタ レベルのバナーを設定します。

`security login banner modify -vserver <cluster_name> { [-message "text"] | [-uri <ftp_or_http_addr>] }`



 a| 
すべての（クラスタおよびSVM）ログインに対してバナーを削除する
 a| 
バナーを空の文字列（``）に設定します：

`security login banner modify -vserver * -message`



 a| 
SVM管理者が作成したバナーを無視する
 a| 
SVMのバナー メッセージを変更します。

`security login banner modify -vserver <svm_name> { [-message "<text>"] | [-uri <ftp_or_http_addr>] }`

|===
* SVMレベルでバナーを管理する場合：
+
SVM コンテキストでは `-vserver <svm_name>`の指定は必要ありません。

+
|===
| 状況 | 操作 


 a| 
クラスタ管理者が提供したバナーをSVMの別のバナーで上書きする
 a| 
SVM用のバナーを作成します。

`security login banner modify -vserver <svm_name> { [-message "text"] | [-uri <ftp_or_http_addr>] }`



 a| 
クラスタ管理者が提供するバナーを抑制し、SVMのバナーが表示されないようにします
 a| 
そのSVMのバナーを空の文字列に設定します。

`security login banner modify -vserver <svm_name> -message`



 a| 
SVM が現在 SVM レベルのバナーを使用している場合は、クラスタレベルのバナーを使用します。
 a| 
SVMバナーを `-`に設定します：

`security login banner modify -vserver <svm_name> -message -`

|===


.関連情報
* link:https://docs.netapp.com/us-en/ontap-cli/security-login-banner-modify.html["セキュリティログインバナーの変更"^]

